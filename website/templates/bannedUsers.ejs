<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Banned</title>
    <link href="/assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/assets/css/style.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.95);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .ban-icon {
            font-size: 100px;
            color: #dc3545;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.8; }
            100% { transform: scale(1); opacity: 1; }
        }

        .card-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #dc3545;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
            margin-bottom: 1.5rem;
        }

        .alert-danger {
            background: linear-gradient(45deg, #ffebee, #ffcdd2);
            border: none;
            border-radius: 15px;
            padding: 1.5rem;
            box-shadow: 0 5px 15px rgba(220, 53, 69, 0.1);
        }

        .alert-danger p {
            margin: 0.5rem 0;
            font-size: 1.1rem;
            color: #c62828;
        }

        .contact-info {
            margin: 2rem 0;
            padding: 1.5rem;
            background: linear-gradient(45deg, #e3f2fd, #bbdefb);
            border-radius: 15px;
        }

        .btn-primary {
            background: linear-gradient(45deg, #2196f3, #1976d2);
            border: none;
            border-radius: 50px;
            padding: 12px 35px;
            font-size: 1.1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(33, 150, 243, 0.3);
        }

        .btn-primary:hover {
            background: linear-gradient(45deg, #1976d2, #1565c0);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(33, 150, 243, 0.4);
        }

        .email-link {
            color: #1976d2;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .email-link:hover {
            color: #1565c0;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body text-center p-5">
                        <i class="fas fa-ban ban-icon mb-4"></i>
                        <h2 class="card-title">Account Banned</h2>
                        <div class="alert alert-danger">
                            <p class="mb-3">Your account has been temporarily suspended.</p>
                            <% if (reason) { %>
                                <p class="mb-3"><strong>Reason:</strong> <%= reason %></p>
                            <% } %>
                            <p><strong>Banned on:</strong> <%= new Date(bannedAt).toLocaleString() %></p>
                        </div>
                        <div class="contact-info">
                            <p class="mb-2">If you believe this was a mistake, please contact our support team:</p>
                            <a href="mailto:Collegeconnect@gmail.com" class="email-link">
                                <i class="fas fa-envelope me-2"></i>Collegeconnect@gmail.com
                            </a>
                        </div>
                        <a href="/logout" class="btn btn-primary mt-4">
                            <i class="fas fa-sign-out-alt me-2"></i>Return to Login
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>